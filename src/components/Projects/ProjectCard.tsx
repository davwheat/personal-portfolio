import React from 'react'

import type { Project } from '../../data/projectList'

interface Props {
  project: Project
}

const ProjectCard: React.FC<Props> = ({ project }) => {
  return <div></div>
}

export default ProjectCard
